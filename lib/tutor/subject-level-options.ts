/**
 * Canonical subjects for tutor profiles (`/tutors/new`) and the tutors map subject filter.
 * Keep this list in sync with what the profile form offers — the map dropdown uses these values only.
 */
export const SUBJECT_OPTIONS = ["dartboard-maths"] as const;

export const LEVEL_OPTIONS = [
  "Pre-Primary School",
  "Primary School",
  "Middle School",
  "High School",
  "University",
] as const;
